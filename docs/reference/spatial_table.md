# Build spatial table from score object

Build spatial table from score object

## Usage

``` r
spatial_table(score, coords, meta = NULL)
```

## Arguments

- score:

  `gleam_score` object.

- coords:

  Coordinate data.frame with x/y.

- meta:

  Optional metadata table.

## Value

Data.frame.
