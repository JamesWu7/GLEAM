# Extract spatial metadata

Extract spatial metadata

## Usage

``` r
extract_spatial_meta(object = NULL, meta = NULL, seurat = TRUE)
```

## Arguments

- object:

  Seurat object.

- meta:

  Metadata for matrix mode.

- seurat:

  Input mode.

## Value

Metadata data.frame.
