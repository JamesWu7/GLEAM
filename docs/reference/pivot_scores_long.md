# Pivot score matrix to long format

Pivot score matrix to long format

## Usage

``` r
pivot_scores_long(score)
```

## Arguments

- score:

  `gleam_score` object.

## Value

Long-format data.frame with `cell_id`, `pathway`, and `score`.
