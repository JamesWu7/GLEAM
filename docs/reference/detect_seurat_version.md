# Detect Seurat major version

Detect Seurat major version

## Usage

``` r
detect_seurat_version(object = NULL)
```

## Arguments

- object:

  Seurat object.

## Value

Integer major version (4, 5) when detectable, otherwise `NA_integer_`.
